1、ID=0: 自己的服务器信息
   ID=-1:就是其父的服务器信息
   ID>1: 就是其子的服务器信息.
2. 服务器为监测中心级:既有自己其父的服务器信息,又有其子的服务器信息,自身信息;
   服务器为装置级:    只有其父级的服务器信息和自身信息;
   服务器为集团监测中心级:只有其子的服务器信息和自身信息;
2007.5.2修改：
装置表中添加两个字段：父级服务器的ID（PARENT_ID），是否有子级(CHILD_FLAG)；


发送实时数据时间间隔单位是“秒”，启停车数据和报警数据间隔单位是分钟。如果间隔值小于1就表示不发送